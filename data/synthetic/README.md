# Synthetic Data (Not Included)

The full synthetic dataset (`synthetic_celiac_data.csv`) is not included in the repository due to file size constraints.
This file can be regenerated by running the following script after downloading the raw data:

```bash
python generate_synthetic_data.py
